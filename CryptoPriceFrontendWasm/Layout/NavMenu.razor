<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">Moneydate</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="price-checker">
                <span class="bi bi-currency-exchange" aria-hidden="true"></span> Monedas
            </NavLink>
            <NavLink class="nav-link" href="bonos">
                <span class="bi bi-currency-exchange" aria-hidden="true"></span> Bonos (Simple)
            </NavLink>
            <NavLink class="nav-link" href="bonos-charts">
                <span class="bi bi-graph-up" aria-hidden="true"></span> Bonos (Histórico)
            </NavLink>
            <NavLink class="nav-link" href="letras">
                <span class="bi bi-currency-exchange" aria-hidden="true"></span> Letras
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
